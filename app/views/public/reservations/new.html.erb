

<%= form_with(model: @reservation, local: true) do |form| %>

    <div class="time">
    <%= form.label :start_time %>
    <%= form.date_select :start_time %>
  </div>

  <div class="submit">
    <%= form.submit %>
  </div>

<% end %>