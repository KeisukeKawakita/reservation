<div class="row">
  <h1>マイページ</h1>
  <div class="col-xs-7">
    <h4>登録情報
	  <%= link_to "編集する", edit_end_users_path, class: "btn btn-primary" %>
	</h4>
	<table class="table table-striped", border="2">
		<tr>
			<th>氏名</th>
			<th>
				<%= @end_user.name %>
			</th>
		</tr>
		<tr>
			<th>カナ</th>
			<th>
				<%= @end_user.name_kana %>
			</th>
		</tr>
		<tr>
			<th>電話番号</th>
			<th>
				<%= @end_user.telephone_number %>
			</th>
		</tr>
		<tr>
			<th>メールアドレス</th>
			<th>
				<%= @end_user.email %>
			</th>
		</tr>
	</table>
  </div>

  <div class="col-xs-5">
    <table class="table">
        自分の予約日
        <% @my_reservations.each do |reservation| %>
          <tr>
            <td><%= reservation.start_time.strftime("%Y-%m-%d %H:%M") %></td>
            <td><%= link_to 'Edit', edit_reservation_path(reservation.id), class: "btn btn-info" %></td>
            <td><%= link_to 'Destroy',reservation_path(reservation.id), method: :delete, data: { confirm: 'Are you sure?' }, class: "btn btn-danger btn-sm" %></td>
          </tr>
        <% end %>
    </table>
  </div>
</div>
